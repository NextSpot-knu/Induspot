# Dataflow 파이프라인 전용 의존성(백엔드 런타임과 분리 — Cloud Run 이미지엔 불필요).
# 의도적으로 apps/api/requirements.txt(=Cloud Run 이미지)에서 제외한다:
# apache-beam 은 별도 관리형 Dataflow 잡으로 실행되므로 API 컨테이너를 무겁게 만들 필요가 없다.
#
# 설치(전용 venv 권장):
#   python -m venv .venv_beam && .venv_beam\Scripts\python -m pip install -r dataflow/requirements.txt
# 검증:
#   python -m pytest dataflow/test_congestion_pipeline.py -q   (DirectRunner)
#
# 버전 핀: 로컬 .venv_beam 에 설치된 apache-beam 2.59.0 과 일치(Python 3.11).
# [gcp] extra 가 Pub/Sub·BigQuery IO 와 DataflowRunner 클라이언트를 끌어온다.
apache-beam[gcp]==2.59.0

# --setup_file 의 sdist 빌드용. 없으면 Beam stager 가 `python -m build` 실패 후
# `setup.py sdist` 로 폴백하지만(동작엔 무방), 설치돼 있으면 1차 경로로 깔끔히 빌드되고
# 런처 실행 시 'No module named build' 경고가 사라진다. (설치: pip install -r dataflow/requirements.txt)
build
