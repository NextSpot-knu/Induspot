# ⚠️ 단일 진실원은 pyproject.toml / poetry.lock 이다.
#   Docker 빌드는 Dockerfile 에서 `poetry export` 로 이 파일을 lock 기준으로 재생성한다.
#   이 목록은 `pip install -r requirements.txt` 로컬 진입점(package.json: api:install/api:dev:local)이
#   깨지지 않도록 pyproject 의 런타임 의존성과 수동 동기화한 것이다. 의존성 변경 시 pyproject 를 고치고
#   가능하면 `poetry export -f requirements.txt --without-hashes -o requirements.txt` 로 다시 생성할 것.

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic[email]>=2.6.4
pydantic-settings>=2.2.1
pyjwt[crypto]>=2.8.0
supabase>=2.3.0
google-cloud-firestore>=2.19.0
python-dotenv>=1.0.1
httpx>=0.27.0
structlog>=24.1.0
scikit-learn>=1.4.0
joblib>=1.3.0
google-cloud-storage>=2.14.0
google-cloud-aiplatform>=1.71.0
google-cloud-bigquery>=3.27.0
google-cloud-pubsub>=2.27.0
google-auth>=2.35.0
google-cloud-secret-manager>=2.19.0
